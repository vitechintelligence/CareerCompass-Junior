# Neon pooled connection string. Configure this in Vercel; never commit a real credential.
DATABASE_URL=postgresql://USER:PASSWORD@HOST/DB?sslmode=require

# Public app URL used for generated links.
NEXT_PUBLIC_APP_URL=http://localhost:3000

# Auth provider configuration will be added after the existing identity setup is confirmed.
